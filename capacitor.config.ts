import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.operationironvengeance.game',
  appName: 'Operation Iron Vengeance',
  webDir: 'dist'
};

export default config;
